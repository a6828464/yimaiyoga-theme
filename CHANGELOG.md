# 更新日志

## v1.7.1 · 2026-09-21
- 修复在线更新校验逻辑：v1.7.0 的完整性校验比对的是**下载到的 zip 字节哈希**，而主更新源（GitHub codeload 分支归档）由服务端按需生成，zip 元数据与发布包不同——同一 commit 两边字节不同（实测 529bee34… vs de8d48ac…），会导致「立即更新」**必然校验失败**。现改为「解压后的内容哈希」（按相对路径 + 文件内容聚合，与压缩实现、时间戳、条目顺序无关），两个来源校验结果一致
- 发布脚本同步改用同一算法生成清单哈希
- 新增 10 项回归断言，锁定「不得退回 zip 字节哈希」

## v1.7.0 · 2026-09-21
安全与数据完整性修复版（依据项目审计报告 AUDIT.md）

### 安全
- 后台默认密码不再可推算：旧版用 `Yimai@+当天日期` 作初始口令，任何人读过源码即可推算并抢先登录。现改为**必须在服务器上运行 `cli/set-admin-password.php` 设置初始密码**；若站点此前从未改过密码，升级时会**自动作废该旧口令**并要求重设（登录页会提示）
- 后台登录增加失败限流（15 分钟内 5 次失败锁定 15 分钟）与会话空闲超时（30 分钟无操作失效），登出时清除会话 cookie
- 在线更新备份不再包含 `inc/local-secrets.php`（此前备份包内含企微 Webhook 与图床授权码，且落在可公开访问的主题目录内）；备份目录移至 `wp-content/yimai-backups` 并加目录级访问拦截
- 更新包增加结构校验、SHA256 校验（发布流程写入 manifest）与**防降级**；`local-secrets.php`、`.backups`、`.update-state.json` 等受保护文件不再被更新包覆盖
- 上传加固：拒绝宽高为 0 的畸形图片（此前触发除零错误致 500）、以服务器真实文件体积判定上限、扩展名只由真实图片类型决定（此前 JPEG 命名 favicon.jpg 会被存成 .png）
- 图床返回值改为严格白名单校验（此前 `/file/` 前缀即可通过，恶意返回值可经内联脚本构成存储型 XSS）
- 内联脚本 JSON 输出统一加脚本上下文转义（`JSON_HEX_TAG` 等）

### 修复
- **后台删除列表项此前不生效并会复制数据**：门店 / 师资 / 课程主题 / FAQ / 导航 / 会员方案 / 培训方案 / 门店空间图 / 活动公告的「删除」保存后会被默认值补回（删 1 条变 8 条、删中间项产生重复）。合并语义改为「列表整体替换、映射逐键合并」，删除与清空如实生效
- 配置类型错误（如关键词被存成字符串）此前会写入数据库并导致**全站白屏且后台自身也打不开**；现在保存时校验类型，不合法则明确提示字段并不写库
- 后台「首页弹窗」总开关此前前台从未读取，关闭后仍会弹出，现已生效
- 每页重复输出两条相同的 meta description
- FAQ 新增项此前以 `{q,a}` 形状写入、前台按 `[0][1]` 元组读取，导致新增条目渲染为空
- 预约表单的门店与体验方向此前硬编码在模板里，现改为读取后台配置
- 预约表单在未配置企业微信 Webhook 时会显示「已提交」但**数据被直接丢弃**；现在无论是否配置 Webhook 都会先落库留存
- 禁用 JavaScript 时首页大面积内容不可见（滚动显现效果无兜底）
- 后台 `/admin` 路径不再硬编码，支持 WordPress 安装在子目录

### 新增
- 预约记录留存（最近 200 条），未配置 Webhook 时预约不再丢失
- Open Graph / Twitter 卡片 / canonical / 本地商家（门店）结构化数据；404 页 noindex
- 后台样式独立为 `assets/css/admin.css`，不再随前台下载

### 优化
- 前台三个样式表（app / fix / mobile-fix）合并为单一 `app.css`：原方案中两个补丁表互相无依赖声明，输出顺序不确定却修改同一批选择器；合并后级联顺序确定（声明逐条等价，已校验）
- `yimai_config()` 增加请求级缓存，避免每次调用重复解析与合并配置
- 发布脚本 `tools/release.sh`：发布前校验工作区干净（此前校验的是 git HEAD 却打包工作区，未提交代码会被静默发布）、校验四处版本号一致、改用 `git archive` 打包（发布物等于已推送 commit，中文文件名 UTF-8 标志与 codeload 一致）、为 manifest 生成 SHA256、重建 Gitee tag（此前只删 release 不删 tag，平台源码包长期停在 v1.1.0）
- `tools/pull-theme.py` 目标路径修正（此前指向不存在的 `tools/yimaiyoga-theme`），并增加工作区检查与 `--dry-run`

## v1.6.1 · 2026-09-12
- 图片库弹窗每次打开自动清空上次的搜索词（此前会保留筛选，误以为图片变少了）

## v1.6.0 · 2026-09-12
- 后台新增「图片库」：更换图片时点「图片库」弹出网格，展示服务器上已上传的全部图片（按时间倒序、可搜索文件名），点击即选用；已同步图床的图片标「图床」角标，缩略图走图床加载，选中后按上传默认设置写入图床/本地地址
- 上传与切换接口返回的图床地址规范化（不再出现 //file/ 双斜杠）
- 主题自带 favicon.png 替换为透明底版本

## v1.5.1 · 2026-09-12
- 修复首页活动弹窗从未真正弹出的问题（弹窗 HTML 输出位置在交互脚本之后，脚本找不到弹窗元素；v1.3.0 引入）

## v1.5.0 · 2026-09-12
- 新增「全站主题」：黑檀米白（默认）/ 青瓷苔绿 / 陶土赤橘 / 黛蓝雾霭 / 玫瑰暖沙，一键切换整个官网的配色（后台界面配色收进同一面板作次要选项）
- 手机端后台改为左侧抽屉菜单：左上角菜单按钮弹出侧滑抽屉，选择后直接进入对应设置；电脑端保持左侧边栏不变
- 预约页活动条新增独立开关（活动公告 → 展示位置）
- 首页弹窗改为「每次进入首页都弹一次」：关闭后本次停留不再打扰，重新进入会再次弹出（此前是每个活动只弹一次，导致再次进入不弹）
- 后台重新上传 Favicon 不再强制铺白底，保留透明
- 移除「原始 JSON」面板

## v1.4.1 · 2026-09-12
- 修复已是最新版本时侧边栏「在线更新」红点仍然显示的问题（hidden 属性被样式覆盖）

## v1.4.0 · 2026-09-12
- 图片管理按页面分组补全：品牌图片（Logo/Favicon/公众号二维码）、首页、预约页、空间页 / 门店；新增「门店空间图」管理（此前预约页与空间页的门店卡片图片在后台无处可改）
- 修复「原始 JSON」编辑保存无效的问题：现在打开该页会同步当前配置，改动并保存后以 JSON 为准，格式错误会拦截并标红
- 新增后台「外观主题」：宣纸暖白（默认）/ 墨夜深色 / 青瓷雅绿 / 陶土暖橘 / 黛蓝雾蓝 五款，点击即时预览，本浏览器记住选择，保存后全设备生效
- 手机端后台适配重做：侧边栏收成横向滑动菜单、保存栏贴合安全区、卡片与表单紧凑化
- 登录页手机端标题不再换行（字号自适应），输入框加大字号避免 iOS 聚焦缩放

## v1.3.1 · 2026-09-12
- 修复后台保存配置时列表项删不掉的问题：门店 / 师资 / 课程主题 / FAQ / 活动公告等列表的「删除」此前会被数据库旧值合并回来，保存的合并基准改为原始默认配置（不含旧值），删除即刻生效

## v1.3.0 · 2026-09-12
- 后台改版：设置页改为左侧分组菜单（站点 / 内容 / 系统），窄屏自动收成横向菜单
- 图片上传支持「本地 / 图床」一键切换，选择后图片地址自动互换；未同步过图床的图片可一键补传
- 前台图片地址改为「所见即所得」：后台存本地就用本地，存图床就用图床（图床加载失败仍自动回退本地）
- 在线更新页新增完整更新日志；发现新版本时高亮显示「新版本更新了什么」，侧边栏出现小红点提醒
- 新增「活动公告」：后台创建活动（支持配图、跳转链接、起止日期），访客首次进入首页弹窗展示一次；预约页顶部自动出现活动条，没有活动时完全隐藏

## v1.2.3 · 2026-09-12
- 更新包下载改用 codeload 分支归档（内容实时生成，彻底规避同名 Release 资产的 CDN 旧包问题）

## v1.2.2 · 2026-09-12
- 修复更新器 backup / apply / migrate 的参数类型错误
- 下载 URL 加时间戳破 GitHub 资产缓存

## v1.2.1 · 2026-09-12
- 图床地址 / 上传密码移入后台可视化配置（数据库优先，local-secrets 兜底）

## v1.2.0 · 2026-09-12
- 后台上传自动同步图床（image.shunan.fun），前台优先图床链接，加载失败自动回退本地

## v1.1.0 · 2026-09-12
- 新增后台在线更新（GitHub + Gitee 双平台）
- 企微 Webhook 密钥移出代码库（inc/local-secrets.php）
- favicon 修复、图片瘦身、去除 Unsplash 外链
